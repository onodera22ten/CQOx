# UI Capture Guide - CQOx NASA/Google++

**Date**: 2025-11-10
**Frontend**: React + TypeScript
**Port**: 4000

---

## 📸 UIキャプチャ取得方法

### 必須条件
```bash
# Docker Composeで全サービス起動
docker-compose up -d

# フロントエンドアクセス
open http://localhost:4000
```

---

## 🎨 主要画面とキャプチャ対象

### 1. **ダッシュボード画面** (Dashboard)

**URL**: `http://localhost:4000/`

**キャプチャ内容**:
- ✅ ジョブ実行状況サマリー
- ✅ リアルタイムメトリクス（Prometheus連携）
- ✅ 最近の推定結果（直近10件）
- ✅ システムヘルスステータス

**主要コンポーネント**:
```typescript
// frontend/src/pages/Dashboard.tsx
<Dashboard>
  <JobsSummary />           // 実行中・完了・失敗のジョブ数
  <MetricsCards />          // メトリクスカード（ATE、CATE、品質スコア）
  <RecentResults />         // 最近の推定結果リスト
  <SystemHealth />          // DB/Redis/Vault接続状態
</Dashboard>
```

**期待される表示内容**:
```
┌─────────────────────────────────────────────┐
│ CQOx Dashboard                    🟢 Healthy│
├─────────────────────────────────────────────┤
│                                             │
│  Jobs Summary                               │
│  ┌──────┐ ┌──────┐ ┌──────┐               │
│  │ 12   │ │ 145  │ │  3   │               │
│  │Running│ │Done  │ │Failed│               │
│  └──────┘ └──────┘ └──────┘               │
│                                             │
│  Metrics                                    │
│  ┌─────────────┐ ┌─────────────┐          │
│  │ ATE: +2.5   │ │ CATE: 0.8-3.2│          │
│  │ (CI: 1.2-3.8)│ │ Coverage: 85%│          │
│  └─────────────┘ └─────────────┘          │
│                                             │
│  Recent Results                             │
│  Job #145 | DID | ATE: +2.5 | ✅ Quality  │
│  Job #144 | RDD | ATE: +1.8 | ✅ Quality  │
│  Job #143 | SCM | ATE: +3.2 | ⚠️  Warning │
└─────────────────────────────────────────────┘
```

---

### 2. **データアップロード画面** (Data Upload)

**URL**: `http://localhost:4000/upload`

**キャプチャ内容**:
- ✅ CSVアップロードフォーム
- ✅ カラムマッピング設定
- ✅ データプレビュー（最初の10行）
- ✅ バリデーション結果

**主要コンポーネント**:
```typescript
// frontend/src/pages/Upload.tsx
<Upload>
  <FileDropzone />          // ドラッグ&ドロップエリア
  <ColumnMapping />         // treatment/outcome/covariates 設定
  <DataPreview />           // アップロードデータプレビュー
  <ValidationStatus />      // データ品質チェック結果
</Upload>
```

**期待される表示内容**:
```
┌─────────────────────────────────────────────┐
│ Upload Dataset                              │
├─────────────────────────────────────────────┤
│                                             │
│  Drop CSV file here or click to browse     │
│  ┌─────────────────────────────────┐       │
│  │                                 │       │
│  │      📁 Drag & Drop Area        │       │
│  │                                 │       │
│  └─────────────────────────────────┘       │
│                                             │
│  Column Mapping                             │
│  Treatment:   [dropdown: treatment      ▼] │
│  Outcome:     [dropdown: revenue        ▼] │
│  Covariates:  [☑] age [☑] region [☑] ...  │
│                                             │
│  Data Preview (First 10 rows)              │
│  ┌─────┬──────┬────────┬─────┐            │
│  │treat│outcome│ age  │region│            │
│  ├─────┼──────┼────────┼─────┤            │
│  │  1  │ 120  │  25  │ Tokyo│            │
│  │  0  │  80  │  30  │ Osaka│            │
│  └─────┴──────┴────────┴─────┘            │
│                                             │
│  ✅ Validation: 1,000 rows, no missing     │
│                                             │
│  [Upload and Run Analysis]                 │
└─────────────────────────────────────────────┘
```

---

### 3. **推定量選択画面** (Estimator Selection)

**URL**: `http://localhost:4000/job/new`

**キャプチャ内容**:
- ✅ 20種類の推定量リスト
- ✅ 各推定量の説明とユースケース
- ✅ パラメータ設定フォーム
- ✅ シナリオ設定（S0/S1）

**主要コンポーネント**:
```typescript
// frontend/src/pages/JobCreate.tsx
<JobCreate>
  <EstimatorGrid />         // 20推定量のグリッド表示
  <EstimatorDetail />       // 選択した推定量の詳細
  <ParameterForm />         // パラメータ入力フォーム
  <ScenarioConfig />        // S0/S1シナリオ設定
</JobCreate>
```

**期待される表示内容**:
```
┌─────────────────────────────────────────────┐
│ Select Estimator                            │
├─────────────────────────────────────────────┤
│                                             │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐│
│  │    DID    │ │    RDD    │ │    SCM    ││
│  │Difference │ │Regression │ │ Synthetic ││
│  │ in Diff   │ │Discontinuity│ │  Control  ││
│  │           │ │           │ │           ││
│  │✓ Panel    │ │✓ Sharp    │ │✓ Panel    ││
│  │✓ Multiple │ │✓ Fuzzy    │ │✓ Staggered││
│  └───────────┘ └───────────┘ └───────────┘│
│                                             │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐│
│  │  2SLS/IV  │ │    PSM    │ │    IPW    ││
│  │Instrumental│ │ Propensity│ │  Inverse  ││
│  │  Variable │ │  Score    │ │Probability││
│  └───────────┘ └───────────┘ └───────────┘│
│                                             │
│  [... 14 more estimators ...]              │
│                                             │
│  Selected: Difference-in-Differences        │
│  ┌─────────────────────────────────┐       │
│  │ Parameters                       │       │
│  │ Time variable:     [date     ▼] │       │
│  │ Group variable:    [region   ▼] │       │
│  │ Parallel trends:   [☑] Check    │       │
│  │ Covariates:        [☑] age ...  │       │
│  └─────────────────────────────────┘       │
│                                             │
│  Scenario: S0 (Observation) vs S1 (Full Intervention)
│                                             │
│  [Run Analysis]                             │
└─────────────────────────────────────────────┘
```

---

### 4. **結果可視化画面** (Results Visualization)

**URL**: `http://localhost:4000/job/:id/results`

**キャプチャ内容** (★最重要):
- ✅ **WolframONE可視化**（S0/S1比較）
- ✅ ATE推定値と信頼区間
- ✅ 品質ゲート結果
- ✅ 自動生成ナラティブ（日本語・英語）
- ✅ 最適ポリシー推奨

**主要コンポーネント**:
```typescript
// frontend/src/pages/Results.tsx
<Results>
  <ATESummary />            // ATE推定値サマリー
  <SmartFigure />           // WolframONE可視化（S0）
  <SmartFigure />           // WolframONE可視化（S1）
  <QualityGates />          // 品質ゲート結果
  <NarrativeSummary />      // 自動生成ナラティブ
  <PolicyRecommendation />  // 最適ポリシー推奨
</Results>
```

**期待される表示内容**:
```
┌─────────────────────────────────────────────┐
│ Analysis Results - Job #145                 │
├─────────────────────────────────────────────┤
│                                             │
│  📊 ATE Estimate: +2.5                      │
│  95% CI: [1.2, 3.8]                         │
│  p-value: 0.001 ***                         │
│  Sample size: 1,000 (500 treated, 500 control)
│                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                             │
│  WolframONE Visualization                   │
│  ┌─────────────────────────────────┐       │
│  │ S0 (Observation)                │       │
│  │                                 │       │
│  │  [Interactive 3D Network Plot]  │       │
│  │   - Nodes: Users                │       │
│  │   - Edges: Influence             │       │
│  │   - Color: Treatment status     │       │
│  │                                 │       │
│  └─────────────────────────────────┘       │
│                                             │
│  ┌─────────────────────────────────┐       │
│  │ S1 (Counterfactual)             │       │
│  │                                 │       │
│  │  [Interactive 3D Network Plot]  │       │
│  │   - All nodes treated           │       │
│  │   - Spillover effects visible   │       │
│  │   - Expected outcome: +3.2      │       │
│  │                                 │       │
│  └─────────────────────────────────┘       │
│                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                             │
│  Quality Gates: ✅ All Passed               │
│  ✅ Parallel trends assumption              │
│  ✅ Covariate balance                       │
│  ✅ No extreme outliers                     │
│  ✅ Sufficient sample size                  │
│                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                             │
│  📝 Auto-Generated Narrative (TL;DR)        │
│                                             │
│  The intervention increased revenue by      │
│  $2.5 per user (95% CI: $1.2-$3.8). This   │
│  effect is statistically significant        │
│  (p < 0.001) and economically meaningful.   │
│                                             │
│  Key Insights:                              │
│  • ROI: 250% (based on $1 intervention cost)│
│  • Spillover: +15% from network effects    │
│  • Risk: Low (narrow confidence interval)  │
│                                             │
│  Strategic Recommendation:                  │
│  1. Scale intervention to full population   │
│  2. Monitor spillover effects               │
│  3. Re-evaluate in 3 months                 │
│                                             │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                             │
│  🎯 Optimal Policy                          │
│                                             │
│  Target: 80% of users (ranked by CATE)      │
│  Expected profit: $1,250 (vs $500 for all)  │
│  Budget: $800 (vs $1,000 for full coverage) │
│                                             │
│  [Download Report] [Export to PDF]          │
└─────────────────────────────────────────────┘
```

---

### 5. **監視ダッシュボード** (Monitoring Dashboard)

**URL**: `http://localhost:4000/monitoring`

**キャプチャ内容**:
- ✅ Prometheusメトリクスの可視化
- ✅ ジョブ実行履歴グラフ
- ✅ エラー率・レイテンシ
- ✅ システムリソース使用状況

**主要コンポーネント**:
```typescript
// frontend/src/pages/Monitoring.tsx
<Monitoring>
  <PrometheusCharts />      // メトリクスグラフ（埋め込み）
  <JobTimeline />           // ジョブ実行タイムライン
  <ErrorRateChart />        // エラー率トレンド
  <ResourceUsage />         // CPU/メモリ使用率
</Monitoring>
```

**期待される表示内容**:
```
┌─────────────────────────────────────────────┐
│ System Monitoring                           │
├─────────────────────────────────────────────┤
│                                             │
│  Prometheus Metrics (Last 1 Hour)           │
│  ┌─────────────────────────────────┐       │
│  │ Request Rate                     │       │
│  │  100 req/min ──────────────────  │       │
│  │                                 │       │
│  │  [Line chart showing requests]  │       │
│  └─────────────────────────────────┘       │
│                                             │
│  ┌─────────────────────────────────┐       │
│  │ API Latency (P95)               │       │
│  │  45ms ──────────────────────────│       │
│  │                                 │       │
│  │  [Line chart showing latency]   │       │
│  └─────────────────────────────────┘       │
│                                             │
│  Job Execution Timeline                     │
│  ┌─────────────────────────────────┐       │
│  │ 14:00 Job#145 ████████ 2.3s ✅  │       │
│  │ 14:05 Job#146 ████████ 1.8s ✅  │       │
│  │ 14:10 Job#147 ███░░░░ 0.5s ⚠️   │       │
│  └─────────────────────────────────┘       │
│                                             │
│  System Health                              │
│  🟢 Database:    Connected (5ms latency)   │
│  🟢 Redis:       Connected (1ms latency)   │
│  🟢 Vault:       Connected                 │
│  🟢 Prometheus:  Collecting metrics        │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 📋 キャプチャ取得手順

### Step 1: サービス起動
```bash
cd /home/user/CQOx

# 環境変数設定
export DB_PASSWORD="secure-password"
export JWT_SECRET_KEY="test-key-32chars-long"
export ENCRYPTION_KEY="test-encryption-key"

# 全サービス起動
docker-compose up -d

# 起動確認（すべてUp表示）
docker-compose ps
```

### Step 2: ブラウザアクセス
```bash
# フロントエンド
open http://localhost:4000

# API
open http://localhost:8080/docs

# Grafana
open http://localhost:3000
```

### Step 3: スクリーンショット取得

**推奨ツール**:
- macOS: `Cmd + Shift + 4` (領域選択)
- Windows: `Win + Shift + S` (Snipping Tool)
- Linux: `gnome-screenshot -a` or `scrot -s`

**保存先**:
```
reports/ui_captures/
├── 01_dashboard.png          # ダッシュボード全体
├── 02_upload.png              # データアップロード画面
├── 03_estimator_grid.png      # 推定量選択画面
├── 04_results_summary.png     # 結果サマリー
├── 05_wolfram_s0.png          # WolframONE可視化（S0）
├── 06_wolfram_s1.png          # WolframONE可視化（S1）
├── 07_narrative.png           # 自動生成ナラティブ
├── 08_policy_recommendation.png # 最適ポリシー推奨
├── 09_monitoring.png          # 監視ダッシュボード
└── 10_grafana.png             # Grafanaダッシュボード
```

---

## ✅ 必須キャプチャ (最低限)

| 画面 | 優先度 | 内容 |
|------|--------|------|
| **結果可視化（WolframONE）** | 🔥 最高 | S0/S1比較の3D/2D図表 |
| **自動ナラティブ** | 🔥 最高 | Beyond NASA/Google機能 |
| **ダッシュボード** | ⭐ 高 | システム全体の状態 |
| **推定量選択** | ⭐ 高 | 20推定量のUI |
| **監視ダッシュボード** | ⚪ 中 | Prometheus統合 |

---

## 🎨 UI技術スタック

```typescript
// package.json (frontend)
{
  "dependencies": {
    "react": "^18.2.0",
    "typescript": "^5.0.0",
    "plotly.js": "^2.26.0",      // WolframONE出力の表示
    "react-dropzone": "^14.2.0", // ファイルアップロード
    "axios": "^1.4.0",           // API通信
    "recharts": "^2.7.0"         // メトリクスグラフ
  }
}
```

---

## 📊 WolframONE可視化の表示

### SmartFigureコンポーネント

```typescript
// frontend/src/components/ui/SmartFigure.tsx

import React, { useEffect } from 'react';

interface SmartFigureProps {
  src: string;        // "reports/figures/ate_density__S0.html"
  caption?: string;   // "S0 (Observation)"
  height?: number;    // 600 (default)
}

export const SmartFigure: React.FC<SmartFigureProps> = ({
  src,
  caption,
  height = 600
}) => {
  return (
    <div className="smart-figure">
      <iframe
        src={src}
        width="100%"
        height={height}
        frameBorder="0"
        style={{ border: '1px solid #e0e0e0', borderRadius: '8px' }}
      />
      {caption && (
        <p className="caption" style={{ textAlign: 'center', marginTop: '8px' }}>
          {caption}
        </p>
      )}
    </div>
  );
};
```

### 使用例

```typescript
// frontend/src/pages/Results.tsx

import { SmartFigure } from '@/components/ui/SmartFigure';

<div className="wolfram-visualizations">
  <h2>WolframONE Visualization</h2>

  <div className="comparison">
    <SmartFigure
      src="/api/figures/ate_density__S0.html"
      caption="S0 (Observation): Actual Treatment Distribution"
    />

    <SmartFigure
      src="/api/figures/ate_density__S1_intervention.html"
      caption="S1 (Counterfactual): Full Intervention Scenario"
    />
  </div>
</div>
```

---

## 🚀 まとめ

### ✅ UIキャプチャ取得のための準備完了

1. **Docker Compose起動**: `docker-compose up -d`
2. **ブラウザアクセス**: `http://localhost:4000`
3. **スクリーンショット**: 10画面を保存
4. **保存先**: `reports/ui_captures/*.png`

### 📊 WolframONE可視化の統合確認

- ✅ `SmartFigure`コンポーネント実装済
- ✅ `.html`形式でインタラクティブ出力
- ✅ S0/S1比較自動生成
- ✅ 42+図表テンプレート

---

**Status**: ✅ **UI & Visualization Integration Ready**

Docker Compose起動後、すぐにUIキャプチャとWolframONE可視化を確認できます。
